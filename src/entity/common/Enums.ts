export enum Type {
	STUDY,
	PROJECT,
}

export enum PostStatus {
	READY,
	PROGRESS,
	PROGRESS_END,
	DONE,
}

export enum TeamMemberStatus {
  READY,
  REJECT,
  ACCEPT,
}

export enum TeamInviteType {
  SELF,
  OTHER,
}